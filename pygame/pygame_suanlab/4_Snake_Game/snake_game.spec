# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(['snake_game.py'],
             pathex=['G:\\내 드라이브\\[SuanLab]\\Publish\\Python Game\\sources\\4 Snake Game'],
             binaries=[],
             datas=[('assets\\NanumGothicCoding-Bold.ttf', 'assets')],
             hiddenimports=[],
             hookspath=[],
             hooksconfig={},
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)

exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,  
          [],
          name='snake_game',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=False,
          disable_windowed_traceback=False,
          target_arch=None,
          codesign_identity=None,
          entitlements_file=None )
